# Backend environment variables for Travel Dashboard
# Copy this file to .env and fill in real values

# Server
PORT=5000
# During local dev, allow CRA dev server origin
FRONTEND_URL=http://localhost:3000
# Use a long random string in production
JWT_SECRET=replace-with-a-strong-secret

# Database (Railway PostgreSQL)
# IMPORTANT: Use the pooled connection URL for DATABASE_URL and the direct URL for DIRECT_URL
# In Railway → Your Postgres → Connect tab:
#  - Copy "Pooled connection (PgBouncer)" into DATABASE_URL and append recommended params
#  - Copy "Postgres Connection URL" (direct) into DIRECT_URL
# Both should include sslmode=require
# Example formats (DO NOT copy these literally):
# DATABASE_URL="postgresql://username:password@oregon-postgres.render.com:5432/railway?pgbouncer=true&connection_limit=1&sslmode=require"
# DIRECT_URL="postgresql://username:password@oregon-postgres.render.com:5432/railway?sslmode=require"
DATABASE_URL=
DIRECT_URL=

# Cloudinary (optional for image uploads)
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=
